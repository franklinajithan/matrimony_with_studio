Public presentation: `/api/plans`, `/api/plans/catalogue`, `/api/plans/terms`, `/api/plans/savings`. Authenticated: `/api/subscription`, `/api/subscription/plan`, `/api/subscription/features`, `/api/entitlements/:feature`, usage/consume routes, billing status/config/prices. Admin reads are under `/api/admin/*`. Live Stripe checkout/webhook/portal remain disabled pending provider credentials/integration.
