import { NextResponse } from 'next/server';import { getServerSubscription } from '@/lib/subscriptions/server';export const dynamic='force-dynamic';export async function GET(){const r=await getServerSubscription();if(!r.user)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({userId:r.user.id,plan:r.plan.code,subscription:r.subscription},{headers:{'Cache-Control':'no-store'}});}
