The repository currently has no Stripe SDK dependency and no production Stripe credentials are available to this implementation. For security, checkout/webhook/portal routes therefore return 503 rather than simulate payment success. This is the only intentionally incomplete part of the billing system; activating it requires external Stripe account configuration and a signature-verified provider implementation.
