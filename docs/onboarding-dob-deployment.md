# Onboarding DOB validation deployment

Step 1 age eligibility is derived from date of birth. The separate `confirmedAdult` value is retained only for backwards-compatible saved drafts and is not required by Step 1 validation.
