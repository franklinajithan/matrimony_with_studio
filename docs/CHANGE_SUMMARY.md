This branch removes the architectural ambiguity between the old Plus model and launch Premium model, adds secure server entitlement resolution and metering, protects billing/admin data with RLS and server authorization, adds admin billing/audit read foundations, and makes Stripe activation explicitly fail closed until real provider credentials and webhook verification are available.
