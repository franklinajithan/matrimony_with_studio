Before production purchase buttons are enabled verify: anonymous subscription APIs return 401; non-admin admin APIs return 403; pending/past_due/canceled/expired subscriptions resolve to Free; expired active periods resolve to Free; browser roles cannot mutate plan/subscription/event/usage tables; free interest #11 is rejected; duplicate Stripe events do not duplicate state transitions; invalid webhook signatures never alter membership; client checkout redirects never alter membership.
