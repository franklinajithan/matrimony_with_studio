Central keys: `advancedFilters`, `fullCompatibility`, `seeWhoLikesYou`, `seeProfileVisitors`, `messagingAfterMatch`, `readReceipts`, `familyIntroduction`, `incognitoMode`, `priorityVisibility`, `premiumBadge`, `prioritySupport`, plus metered `interestsPerMonth`, `profileSharesPerMonth`, `biodataTemplates`, `profileBoostsPerMonth`. New paid features should add one key here/`PlanEntitlements` rather than hard-code plan-name checks in components.
