Pure-domain tests cover launch prices, plan codes, legacy Plus normalization, paid-status fail-closed behavior, entitlement decisions, usage limits, upgrade ordering and pricing savings. GitHub Actions runs TypeScript checking and a production Next build for pull requests. Database/RLS and Stripe test-mode integration tests remain deployment checks because they require Supabase/Stripe environments.
