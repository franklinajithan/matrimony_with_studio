import { NextResponse } from 'next/server';import { requireServerAdmin } from '@/lib/subscriptions/server';export async function GET(){if(!await requireServerAdmin())return NextResponse.json({error:'Forbidden'},{status:403});return NextResponse.json({plans:'subscription_plans',subscriptions:'member_subscriptions',events:'subscription_events',usage:'subscription_usage',audit:'admin_audit_log'});}
